package org.sship.starship.commandpattern;

/**
 * The ICommand interface represents a command in the command pattern.
 * It defines a method to execute the command.
 */
public interface ICommand {
    /**
     * Executes the command.
     */
    void execute();
}

